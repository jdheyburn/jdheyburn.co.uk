name: Markdown lint

on:
  push:
    branches-ignore:
      - "master"

jobs:
  build:
    name: Lint code base
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Lint Code base
        uses: github/super-linter@v2.0.0
        env:
          VALIDATE_MD: true
